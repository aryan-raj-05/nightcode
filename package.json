{
  "name": "nightcode",
  "version": "1.0.0",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev:cli": "bun run --watch packages/cli/src/index.tsx",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
